# VLT-5G 결과지 저장소 작업 지침

## 작업 시작 전 자동 확인 — 최우선

이 저장소에서 새 요청을 받으면 작업 형식 추정, 스킬 선택, 파일 생성·수정, Git 작업보다 먼저 적용 범위의 `AGENTS.md`를 끝까지 읽는다.

1. 현재 디렉터리부터 저장소 루트까지 상위 폴더를 확인한다.
2. 발견한 모든 `AGENTS.md`를 상위에서 하위 순서로 적용한다.
3. 현재 폴더에 파일이 없다는 이유로 지침이 없다고 판단하지 않는다.
4. 사용자가 `agent.md`, `agents.md`, `에이전트 지침`이라고 말해도 `AGENTS.md`를 뜻하는 것으로 해석한다.
5. 지침 확인 전에는 DOCX·PDF·HTML 등 산출물 형식을 임의로 정하지 않는다.

## 저장소와 브랜치

- 이 디렉터리가 실제 Git 저장소 루트다.
- 기본·배포 브랜치는 `master`다.
- GitHub Pages는 `master` 루트에서 배포된다.
- 작업 전 `git fetch origin`과 `git status -sb`로 원격 및 로컬 상태를 확인한다.
- 다른 작업자의 변경을 덮어쓰거나 `git reset --hard`, 강제 푸시를 사용하지 않는다.

## `학생ID 작성` 명령 해석

사용자가 `박시현12 작성`처럼 학생 식별자와 함께 `작성`이라고 요청하면 별도 형식을 묻지 않고 학생별 VLT-5G HTML 결과지 전체 작업을 실행한다.

기본 완료 범위는 다음과 같다.

1. 학생 VLT-5G 보고서, 녹취, 학생·학부모 검사지, 관리자 정보, 국어·수학·영어 APT를 확인한다.
2. 저장소 밖 로컬 완성본 HTML과 내부 운영 기록을 만든다.
3. 저장소 안에 학부모용·매니저용 각각의 중복되지 않는 ASCII 8자리 슬러그 폴더와 `index.html`을 만든다.
4. 관리자 목록에 두 링크를 추가한다.
5. 초기상담 Google Sheets의 올바른 학생 탭을 작성하고 재조회한다.
6. 해당 학생 관련 배포 파일만 커밋해 `origin/master`에 푸시한다.
7. 방금 푸시한 커밋의 Pages 빌드가 `built`인지 확인한다.
8. 학부모용·매니저용 URL이 HTTP 200이고 최신 학생 내용과 `noindex`를 포함하는지 확인한다.

사용자가 `초안`, `로컬만`, `배포하지 말 것` 등 범위를 명시하면 그 요청을 우선한다. DOCX·PDF는 사용자가 명시적으로 요구한 경우에만 만든다.

## 학생·학부모 검사지와 TIRO 구두 응답

- 학생용·학부모용 검사지가 별도 파일로 전달되지 않았다는 이유만으로 자료 누락으로 판단하지 않는다.
- 길음점 초기상담에서는 TIRO 녹취 중 학생과 학부모에게 검사지 문항을 구두로 묻고 답을 확인하는 경우가 대부분이므로, 대상 학생의 TIRO 전체 녹취를 먼저 확인한다.
- 녹취에서 학생 본인 관점과 학부모 관점을 분리해 포집한다. 성격, 강점, 보완점, 관심사, 부모 친밀도·스트레스, 학습 방해 요소, 계획·시험 준비, 현재 성적, 목표, 진로, 사교육·일정 등에 대한 문답은 해당 관점의 검사지 응답 근거로 사용할 수 있다.
- 별도 검사지와 TIRO 구두 응답이 모두 있으면 서로 대조한다. 충돌 시 어느 자료의 발언인지 구분하고, 상담 중 최신 확인·정정된 내용을 우선하되 임의로 합치지 않는다.
- 별도 검사지가 없지만 TIRO에서 구두 문답이 확인되면 이를 정상적인 검사 근거로 취급하며, 학부모용 결과지에 `검사지 누락`이나 제작 과정 설명을 노출하지 않는다.
- 학생 또는 학부모 한쪽의 관점만 확인되면 확인된 관점만 사용하고 다른 관점의 내용을 추정하지 않는다.
- 별도 검사지에도 없고 TIRO 전체 녹취에도 학생·학부모 검사지에 해당하는 내용이 아예 없으면, 필수 자료가 없음을 사용자에게 알리고 필요한 자료를 요청하거나 해당 관점 제외 승인을 받는다. 승인 전에는 없는 내용을 만들어 HTML이나 초기상담 시트에 채우지 않는다.
- 내부 운영 기록에는 근거가 별도 검사지인지 TIRO 구두 응답인지 식별 가능하게 남긴다.

## `APT 추가` 명령 해석

사용자가 `[주시현09 APT 국어] URL`처럼 학생ID·과목·Google Sheets 링크를 전달하고 `APT 추가`라고 요청하면 별도 형식을 묻지 않고 관리자 APT 링크 등록과 배포까지 실행한다. 채팅 발신자명과 시각 표시는 자료가 아니므로 무시하고, 대괄호 안의 학생ID와 과목을 링크에 대응시킨다.

기본 처리 절차는 다음과 같다.

1. 전달된 링크를 학생ID별 국어·수학·영어로 구조화하고, 링크에서 Google Sheets 문서 ID를 추출한다.
2. Google Sheets 메타데이터를 읽어 각 링크가 열리는지, 문서 제목의 학생명과 과목이 요청과 일치하는지 확인한다. 학교·학년 표기가 있으면 함께 대조하되, 사용자 학생ID의 출생연도 식별자는 임의로 바꾸지 않는다.
3. 열리지 않는 링크, 학생명·과목 불일치, 같은 문서 ID의 중복 배정이 있으면 잘못된 링크를 등록하지 않고 해당 항목을 사용자에게 알린다.
4. `admin-4ck2v7p3/index.html`의 `aptSheets` 배열에서 학생ID와 문서 ID를 먼저 검색해 중복을 확인한다.
5. 신규 학생은 국어·수학·영어 링크가 모두 있을 때 학생 1명당 객체 1개로 추가한다. 신규 학생인데 일부 과목만 전달되면 나머지를 추정하거나 빈 링크로 만들지 않고 누락 링크를 요청한다.
6. 기존 학생은 전달된 과목의 링크만 갱신하고, 전달되지 않은 과목의 기존 링크는 그대로 유지한다. 동일한 학생ID 객체를 추가로 만들지 않는다.
7. 사용자가 링크 등록만 요청한 경우 APT 원본 Sheets의 셀은 수정하지 않고 읽기 전용으로 검증한다. 결과지 생성, APT 점수 분석, 초기상담 시트 입력도 함께 요청되지 않았다면 확대 수행하지 않는다.
8. 관리자 페이지의 기존 `noindex, nofollow, noarchive`와 내부용 안내를 유지한다.
9. `git diff --check`와 `scripts/verify-repository.ps1`을 실행하고, 이번 요청으로 바뀐 파일만 명시적으로 스테이징한다.
10. `git diff --cached --check`와 스테이징 내용을 검토한 뒤 커밋해 `origin/master`에 푸시한다.
11. 최신 GitHub Pages 빌드의 커밋이 방금 푸시한 커밋과 같고 상태가 `built`인지 확인한다.
12. 관리자 공개 URL이 HTTP 200인지, 새 학생ID와 모든 전달 문서 ID 및 `noindex`가 실제 배포 HTML에 포함됐는지 확인한다.

완료 시 등록한 학생과 과목 수, 커밋 해시, Pages 상태, 검증한 관리자 URL을 보고한다. 사용자가 `로컬만`, `배포하지 말 것` 등 범위를 제한하면 그 요청을 우선한다.

## `후속상담 HTML 작성` 명령 해석

사용자가 학생 식별자와 함께 `후속상담 HTML 작성`, `후속상담 작성`, `후속상담 세션 추가`, `후속상담 포스팅`, `후속상담 배포`, `면담 기록 HTML로 올려`처럼 요청하면 기존 학생의 admin 행에 내부 운영용 후속상담 회차를 추가하고 정적 HTML을 배포하는 작업으로 해석한다.

기본 처리 절차는 다음과 같다.

1. 학생명·상담일·제공 원고가 기존 admin 학생과 일치하는지 확인한다.
2. 확정 원본은 private 저장소의 `내부용`에 보존하며, 공개 저장소에는 공개 가능한 정적 HTML만 둔다.
3. 세션마다 중복되지 않는 랜덤 ASCII 8자리 슬러그 폴더와 `index.html`을 새로 만든다. 초기상담 결과지나 매니저 기록지의 기존 슬러그를 재사용하지 않는다.
4. 제공된 최종 HTML이나 확정 원고의 사실 내용은 임의로 바꾸지 않는다. 사용자가 디자인 보완을 요청하면 브랜드 헤더, 섹션 탐색, 모바일, 인쇄 스타일 등 표현만 개선한다.
5. 내부 페이지에 `noindex, nofollow, noarchive`, 내부 운영용 안내, `../admin-4ck2v7p3/` 복귀 링크를 유지한다.
6. `admin-4ck2v7p3/index.html`의 `followupSessions`에서 기존 학생 객체의 `sessions` 배열에 `{ date, title, href }`를 시간순으로 추가한다. 기존 학생 객체를 중복 생성하지 않으며, 신규 학생일 때만 객체를 하나 만든다.
7. 한 학생에게 여러 세션이 있으면 admin의 같은 학생 행에 회차순으로 모두 표시되도록 한다.
8. private 저장소의 `결과지\배포_기록.md` 후속상담 세션 매핑표에 학생·회차·상담일·제목·슬러그·원본 경로를 기록한다.
9. 공개 저장소에는 이번 세션 HTML과 admin 변경만 명시적으로 스테이징하고 `git diff --check`, `git diff --cached --check`, `scripts/verify-repository.ps1`을 통과시킨다.
10. `origin/master`에 push한 뒤 최신 Pages 빌드가 방금 커밋으로 `built`인지 확인한다.
11. admin URL과 세션 URL이 HTTP 200인지, admin에 학생·세션 경로가 포함되는지, 세션 HTML에 제목·내부 표시·admin 복귀 링크·`noindex`가 포함되는지 검증한다.
12. 완료 시 admin URL, 세션 URL, 커밋 해시, Pages 상태를 보고한다.

사용자가 `초안`, `로컬만`, `배포하지 말 것`, `디자인만`처럼 범위를 제한하면 그 요청을 우선한다. `noindex`는 접근 제어가 아니므로 연락처·검사지·녹취 원문 등 공개 저장소에 둘 수 없는 개인정보를 세션 HTML에 포함하지 않는다.

## 타입 확인

- 메인 타입과 히든 타입은 원본 보고서·녹취·로컬 타입 기준표를 대조한다.
- 번호만 보고 별칭을 추측하지 않는다.
- 학부모 HTML 상단에 `메인 TYPE N. 정식명칭`, `히든 TYPE N. 정식명칭`을 구분해 표시한다.
- 성장 방향 문구를 히든 타입 자리에 대신 넣지 않는다.
- 학부모용 HTML, 내부 운영 기록, 매니저용 HTML의 타입 표기가 일치해야 한다.

## 학부모용 HTML

- 표준은 `ifirlhkv/index.html`의 `REPORT_DATA` 기반 13개 섹션, 레이아웃, 카드·표·그래프, 모바일·인쇄 스타일이다.
- 학생별 데이터와 분석만 교체하고 임의 축약 템플릿을 만들지 않는다.
- VLT 세목의 `장점`, `보완점`, `발전할 점`을 빠짐없이 포집해 `검사 결과지 기반` 핵심 요약으로 연결한다.
- 국어·수학·영어 APT 상위 지표를 동일한 0~5 척도와 기준선으로 재구성한다.
- 점수 나열에서 끝내지 않고 `시각적 근거 → 현재 작동 방식 → 학습 리스크 → 실행 처방`으로 서술한다.
- APT 강점 지표와 우선 보완 지표는 서로 다른 고정 색상을 쓰고 범례에도 같은 실제 색상 표식을 사용한다. 각 해당 행에는 `강점`, `우선 보완` 텍스트 배지를 함께 표시한다.
- 학부모용에는 관리자 링크, APT 원본 URL·문서 ID, 전화번호, 제작 과정, 자료 목록, 원본 확인 상태, 누락 변명을 넣지 않는다.
- 확인되지 않은 필수 자료를 추정해 채우지 않는다. 사용자에게 필요한 자료를 요청하거나 제외 승인을 받는다.
- `상담 합의`에는 학생·학부모가 녹취에서 명시적으로 동의한 실행 사항만 넣는다. 학교·학년, 검사 타입, 점수, 진로 관심은 기본정보·확인 사실이지 합의가 아니다.
- 상담 결과는 `학생·학부모 확인`, `검사·상담 진단`, `상담자 권고`, `가족 결정 대기`, `추후 검토`, `최종 합의`로 구분하며, 권고나 미결정 상태를 합의로 승격하지 않는다.

## 학부모용 공유 미리보기 메타데이터

- 모든 학부모 공개용 `index.html`은 카카오톡·문자·메신저 공유 카드 제목이 학생별로 보이도록 문서 제목과 Open Graph 메타를 설정한다.
- 기본 공유 제목 형식은 `VLT-5G 초기 진단 결과지_[학생명][학년/식별자]`로 한다. 예: `VLT-5G 초기 진단 결과지_장하서12`.
- 같은 제목을 최소한 `<title>`, `meta[name="description"]`, `meta[property="og:title"]`, `meta[property="og:description"]`, `meta[name="twitter:title"]`, `meta[name="twitter:description"]`에 반영한다.
- `meta[property="og:url"]`에는 최종 학부모용 공개 URL을 정확히 넣는다.
- 런타임 스크립트에서 `document.title`을 설정하는 경우 위 공유 제목과 같은 값으로 맞춘다.
- 배포 검증 때 공개 URL을 다시 읽어 `<title>`과 `og:title`이 위 형식으로 내려오는지 확인한다. 카카오톡 캐시 때문에 기존 카드가 남을 수 있으므로, 필요하면 같은 공개 URL에 `?v=YYYYMMDD-title` 형태의 캐시 갱신용 쿼리 URL을 함께 안내한다.

## Google Sheets — 박시은 탭 수준 유지

초기상담 기록지는 `박시은11_숭곡중_임` 탭을 셀 배치와 정보 밀도의 품질 기준으로 삼는다. 박시은의 학생 데이터는 복사하지 않고 대상 학생의 근거 자료만 사용한다.

1. 대상 학생 탭이 없으면 작업을 중단하지 않고 `샘플` 시트의 사본을 만든다. 탭 이름은 학생ID·실제 학교·담당자를 확인해 정확하게 지정한 뒤 작성한다.
2. 값을 쓰기 전에 대상 탭과 기준 탭의 같은 범위를 서식 포함 셀 읽기로 확인한다.
3. 병합 구조, 라벨 셀, 값 시작 앵커, 수식, 데이터 유효성 검사를 비교한다. 평문 범위 값만으로 입력 위치를 추측하지 않는다.
4. `학생이름 | 값`, `학부모 니즈 | 값`, `국어 | 분석`처럼 라벨 셀에 값을 합치지 않는다.
5. 라벨은 기존 라벨 셀에 보존하고 데이터는 지정된 흰색 값 셀의 좌상단 앵커에 쓴다.
6. 섹션 제목과 전략 헤더를 상세 내용으로 덮어쓰지 않는다.
7. 학습현황 진단은 과목별 사교육, 일정, 진도, 내신, 모의고사, 교재, 목표·특이사항을 근거 범위에서 구체화한다.
8. 확인되지 않은 값은 사실을 만들지 않고 `미확인`, `해당 없음`, `세부 요일·시간 미확인`처럼 상태를 적는다.
9. APT는 국어·수학·영어 라벨과 분석 셀을 분리하고 상위 지표, 강점, 우선 보완점을 함께 적는다.
10. 자기주도 진단은 기준 탭의 실제 병합 구조를 따른다. 별도 답변 앵커가 없는 질문 셀에 `|`로 답변을 붙이지 않는다.
11. 초기 3개월 전략은 헤더 행과 상세 내용 행을 분리한다.
12. 작성 후 같은 범위를 다시 읽어 라벨·값 분리, 섹션 제목, APT 3과목, 자기주도 진단, 전략 헤더·내용, 과목별 주요 칸을 검증한다.

## 공개·개인정보 원칙

- 저장소와 Pages는 공개 상태임을 전제로 한다.
- 학부모용 페이지에는 학생·학부모 연락처를 넣지 않는다.
- 매니저용 페이지와 관리자 목록은 내부 운영용으로 표시하고 `noindex, nofollow, noarchive`를 적용한다.
- `noindex`는 접근 제어가 아니므로 내부 링크를 학부모에게 공유하지 않는다.
- 학생 이름을 폴더명에 사용하지 않고 기존 학생 슬러그는 유지한다.

## 커밋·푸시·배포 검증

1. `git diff --check`를 실행한다.
2. 이번 작업 범위의 파일만 명시적으로 스테이징한다.
3. `git diff --cached --check`와 스테이징 내용을 검토한다.
4. `master`가 원격보다 뒤처졌으면 안전하게 원격 변경을 반영한다.
5. `git push origin master`를 실행한다.
6. 최신 Pages 빌드의 커밋이 푸시한 커밋과 같고 상태가 `built`인지 확인한다.
7. 변경된 모든 URL의 HTTP 200과 실제 내용을 확인한다.
8. 학부모용 공개 URL의 `<title>`과 `og:title`이 `VLT-5G 초기 진단 결과지_[학생명][학년/식별자]` 형식으로 설정되어 있는지 확인한다.

완료 시 커밋 해시, Pages 상태, 검증한 URL을 보고한다.
